# 2026-04-28

## Что сделано

- Реализован Gemini-клиент (`app/llm/gemini_client.py`): тонкая обёртка над `google-genai`, классификация ошибок на permanent (`GeminiAPIError`) и transient.
- Реализована LLM-логика для `resolve_keys` (`app/llm/resolve_keys_llm.py`): вызов Gemini Flash со Structured Outputs, возврат `{new_keys, resolved_schema}`.
- Реализована LLM-логика для `extract` (`app/llm/extract_llm.py`): динамическая Pydantic-модель через `create_model()`, вызов Gemini Pro, возврат плоского `{key_name: value_or_null}`.
- Реализован Celery-воркер `resolve_keys` (`app/workers/resolve_keys.py`): кастомный lifecycle без скачивания файла из MinIO.
- Реализован Celery-воркер `extract` (`app/workers/extract.py`): заменил заглушку, использует `run_document_task` + closure для передачи `extraction_schema`.
- Обновлён `app/celery_app.py`: добавлен `app.workers.resolve_keys` в `include`, добавлен роутинг в очередь `llm`.
- Написаны тесты: `tests/test_resolve_keys_worker.py` (17 тестов), `tests/test_extract_worker.py` (17 тестов).
- **Архитектурная чистка**: удалён `md_document_id` из `resolve_keys` полностью — из LLM-функции (`resolve_keys_llm.py`), воркера (`workers/resolve_keys.py`) и тестов. Все тесты продолжают проходить.
- **Code review**: разобраны замечания CodeRabbit и Copilot, исправлены все найденные проблемы:
  - `Optional[str]` → `str | None` везде в `extract_llm.py` (UP045)
  - Порядок импортов в `extract_llm.py` и `tests/test_resolve_keys_worker.py` (I001)
  - Удалён неиспользуемый `GeminiAPIError` из `workers/extract.py`, `call` из тестов (F401)
  - Объединены вложенные `with` в тестах через `pytest.raises` как контекст-менеджер (SIM117)
  - Добавлена валидация `extraction_schema` в `_build_extraction_model()`: `ValueError` на non-dict элемент, отсутствующий или пустой `key_name` — теперь битые kwargs от Go не уходят в 3 ретрая
  - Очищены docstring-расхождения: убраны упоминания `md_document_id` из `workers/extract.py`, исправлен пример lifecycle в `CLAUDE.md`, убран Presidio из stack (используется только Natasha + stdnum + regex)
  - Добавлена `@pytest.mark.integration` на классы/методы, тестирующие lifecycle с мокнутым Go/MinIO (`TestExtractHandle`, `TestRunResolveKeys`, `TestResolveKeysTaskValidation`, два метода в `TestExtractTaskValidation`) — затем **убрана** по замечанию: все эти тесты используют только моки и не требуют внешних сервисов; оставлять их в `integration` значит исключать из `make test-fast` без причины
  - Добавлена cardinality-проверка в `resolve_keys_llm.py`: если Gemini вернул не то количество ключей, что вопросов — `ValueError` с деталями расхождения; тест `test_cardinality_mismatch_raises_value_error`
  - `_ResolvedKeyItem.key_name` в `resolve_keys_llm.py` валидируется через `@field_validator` по паттерну `^[a-z][a-z0-9_]*$` — невалидный ответ Gemini падает сразу при парсинге
  - `_build_extraction_model` в `extract_llm.py` проверяет `key.isidentifier()` — защита `create_model()` от ключей вида `"total-square"` или `"2bad"`
  - Порядок в `_run_resolve_keys`: валидация `raw_questions` теперь идёт **до** `get_client()` — битые kwargs не тратят время на инициализацию клиента
  - Добавлена цель `make ci` (format → check → test) в Makefile
  - `go.update_task(status="processing")` в `_run_resolve_keys` перемещён **до** `get_client()`: если SDK упадёт при инициализации — Go получит `processing` → `failed` в правильном порядке согласно docstring
  - `_build_prompt` в `resolve_keys_llm.py`: `existing_keys` всегда сериализуется через `json.dumps` (валидный JSON `[]`), пояснительный текст для модели вынесен в отдельную переменную `existing_keys_note` — раньше в секции стояло `"[]  (no existing keys — all questions are new)"`, что ломало JSON-ожидания
  - `resolve_keys()` в `resolve_keys_llm.py`: добавлена валидация типов элементов перед вызовом Gemini — каждый элемент `raw_questions` должен быть непустой строкой, каждый элемент `existing_keys` — словарём со строковыми значениями; защищает от кривых kwargs от Go до траты LLM-бюджета
  - Добавлены `isinstance(..., list)` проверки перед итерацией в `resolve_keys()`: строка `"вопрос"` проходила старую валидацию (каждый символ — `str` с непустым `.strip()`); теперь явно проверяется тип контейнера
  - `_run_resolve_keys` в `workers/resolve_keys.py`: добавлена `isinstance(raw_questions, list)` проверка до `go.update_task()` — строка-вместо-списка падает до инициализации Gemini SDK
  - `gemini_client.py`: `resp.text` обрезается до 200 символов в сообщении `GeminiAPIError` — полный ответ LLM не уходит в `error_message` Go и не засоряет логи
  - Покрыта ветка `key.isidentifier()` в `_build_extraction_model`: тесты `test_invalid_identifier_key_name_raises_value_error` (`"total-square"`) и `test_digit_leading_key_name_raises_value_error` (`"2bad"`)
  - Покрыта ветка `@field_validator` в `_ResolvedKeyItem.key_name`: тесты `test_invalid_key_name_from_llm_raises_value_error` (`"Total-Square"`) и `test_digit_leading_key_name_from_llm_raises_value_error` (`"2bad"`)
  - `resolve_keys_llm.py`: расширена валидация элементов `existing_keys` — проверка наличия всех трёх обязательных полей (`key_name`, `source_query`, `data_type`), строковых типов значений, допустимых значений `data_type` (`"string"`, `"number"`, `"date"`); константы вынесены на уровень модуля (`_REQUIRED_EXISTING_KEY_FIELDS`, `_ALLOWED_EXISTING_DATA_TYPES`) — ruff N806 не даёт `UPPER_CASE` переменным внутри функции
  - `extract_llm.py`: добавлена проверка `isinstance(data_type, str)` в `_build_extraction_model` — без неё `list` или `dict` в `data_type` вызывал `TypeError: unhashable type`, который не входит в `_NO_RETRY` и уходил в 3 ретрая
  - `Makefile`: исправлена help-строка цели `worker-llm` — добавлен `resolve_keys` в список очередей
  - `extract_llm.py`: добавлена проверка дублирующихся `key_name` в `_build_extraction_model` — если Go прислал два entry с одинаковым ключом, `dict` молча перезаписывал бы поле; теперь `ValueError("... is a duplicate")` до `create_model()`
  - Исправлен тест `test_non_string_value_in_existing_keys_raises_value_error` в `test_resolve_keys_worker.py` — entry не содержал `source_query`, тест падал раньше на "missing required field" вместо "must be a string"; добавлен `"source_query": "s"`
  - `celery_app.py`: убран "Presidio" из комментария к очереди `llm` — стек использует только Natasha
  - `workers/resolve_keys.py`: исправлен docstring (порядок шагов жизненного цикла); `kwargs.get("existing_keys") or []` → `kwargs.get("existing_keys", [])` — falsy-значения вроде `""` или `{}` теперь доходят до `isinstance`-проверки и дают явный `ValueError` вместо молчаливого превращения в `[]`
  - `extract_llm.py`: неизвестный `data_type` теперь `ValueError("is not supported")` вместо silent fallback на `str | None`; тест `test_unknown_data_type_defaults_to_optional_str` → `test_unknown_data_type_raises_value_error`
  - `resolve_keys_llm.py`: при построении `resolved_schema` `data_type` берётся из `existing_keys_by_name` если ключ уже существует — LLM не может изменить тип данных зарегистрированного поля
  - `gemini_client.py`: переключён на новый Structured Outputs API (`response_json_schema=Schema.model_json_schema()` + `Schema.model_validate_json(resp.text)`) вместо устаревшего `response_schema=PydanticClass` + `resp.parsed`; теперь `ValidationError` явно ловится и преобразуется в `GeminiAPIError`
  - Модели обновлены: `GEMINI_LIGHT_MODEL` → `gemini-2.5-flash`, `GEMINI_HEAVY_MODEL` → `gemini-3.1-pro-preview`
- Итоговый пробег: **204 теста**, все проходят, `ruff check` чист.
- **Code review PR #5** — разобраны и применены замечания ревьюеров:
  - `gemini_api_key: str = ""` → `Field(..., min_length=1)` в `config.py` — fail-fast при старте без ключа.
  - Proxy URL редактируется перед логом через `urlparse` — в лог уходит только `scheme://hostname`, без `user:pass@`.
  - `GeminiClient.close()` + `try/finally` в воркерах `resolve_keys` и `extract` — `httpx.Client` закрывается гарантированно.
  - Создан `tests/test_gemini_client.py` — 8 юнит-тестов для `generate()`: happy path, пустой ответ, несовпадение схемы, permanent/transient ошибки, `close()` без прокси.
  - `tests/conftest.py` дополнен `GEMINI_API_KEY=test-gemini-key`.
  - `scripts/resolve_keys_check.py`: добавлен `isinstance(k, dict)` guard после `isinstance(existing_keys, list)`.
- Итого после всех правок: **212 тестов**, `make ci` чист.

**Константы модульного уровня вместо локальных в `resolve_keys_llm.py`** — ruff N806 запрещает `UPPER_CASE` переменные внутри функций (они воспринимаются как константы, а не локальные). `_REQUIRED_EXISTING_KEY_FIELDS` и `_ALLOWED_EXISTING_DATA_TYPES` вынесены на уровень модуля — они всё равно не зависят от параметров вызова.

**`isinstance(data_type, str)` перед `_FIELD_TYPE.get()`** — если Go прислал `extraction_schema` с `{"key_name": "total", "data_type": ["string"]}`, Python ловил бы `TypeError: unhashable type: 'list'` на `dict.get()`. Этот тип не в `_NO_RETRY`, значит 3 ретрая с одним и тем же сломанным вводом. Явная проверка с `ValueError` решает это.

**Кастомный lifecycle для `resolve_keys`** — задача не скачивает никаких файлов. Все входные данные приходят через `kwargs` из Go. Использование `run_document_task` было бы лишним (оно всегда делает `minio.download()`). Написан отдельный `_run_resolve_keys()` с теми же гарантиями (processing → completed/failed, retry).

**Closure-паттерн для `extract`** — `_handle()` нужен доступ к `extraction_schema` из kwargs, но `run_document_task` ожидает `handler(file_bytes, storage_path, minio)`. Решение: `_bound_handle = lambda file_bytes, sp, minio: _handle(file_bytes, sp, minio, extraction_schema)`. Это позволяет переиспользовать готовый `run_document_task` без изменения его сигнатуры.

**`Optional[str]` для всех полей extract** — суммы в русских договорах ("1 500 000 руб. с НДС") нельзя надёжно парсить во float. Go хранит всё как текст в `document_extracted_data.value`. Gemini может вернуть `null` если значение не найдено — Python оставляет как `None`, Go пропускает при сохранении.

**Удаление `md_document_id` из `resolve_keys`** — изначально Python принимал `md_document_id` как обязательный kwarg, никак не использовал в LLM-логике и возвращал обратно в `result_payload`. Это чистый pass-through антипаттерн: Go сам знает, к какому документу относится задача — ему не нужен Python как зеркало. После удаления:
- Go больше не должен делать `ListDocumentsByParent` перед публикацией `resolve_keys_task`
- Kwargs: только `raw_questions: list[str]` и `existing_keys: list[dict]`
- `result_payload`: только `{"new_keys": [...], "resolved_schema": [...]}`
- Убрано 2 теста-валидации для `md_document_id`, добавлен `test_result_has_no_md_document_id`

## Ловушки и особенности

**`'{"wrong_field": 123}'` не проверяет несовпадение схемы надёжно** — Pydantic v2 по умолчанию игнорирует лишние поля (`extra="ignore"`), поэтому `wrong_field` молча проглатывается. `ValidationError` летит только потому что обязательное поле `value` отсутствует. Если добавить `value: str = "default"`, тест тихо перестаёт проверять то, что заявляет. Надёжнее передавать структурно невалидный payload — массив вместо объекта (`"[1, 2, 3]"`): такой JSON никогда не десериализуется в Pydantic-модель, независимо от полей и дефолтов.

**`urlparse(proxy_url).hostname` vs `.netloc`** — `.hostname` возвращает только имя хоста без порта и без credentials. `.netloc` вернул бы `user:pass@host:port`. При логировании нужен именно `.hostname`.

## Решения и обоснования — Celery уже привязывает `self` в `.run()`, поэтому в тестах нужно вызывать `task.run(task_id, doc_id, storage_path, **kwargs)` без явного `self`. `.__wrapped__` не работает с `**kwargs` (принимает фиксированный список аргументов).

**`GeminiAPIError` наследует `ValueError`** — поэтому попадает в `_NO_RETRY = (GoClientError, GeminiAPIError, ...)` без дополнительной логики. `ValueError` из валидации kwargs тоже не ретраится (тот же тип), что правильно: неверные kwargs — это ошибка Go, ретрай бессмысленен.

**`str | None` вместо `Optional[str]`** — Python 3.12 поддерживает union-синтаксис нативно. `Optional[str]` — это алиас из `typing`, который ruff (UP045) помечает как устаревший. `_FIELD_TYPE` в `extract_llm.py` теперь использует `str | None` напрямую. `from typing import Optional` удалён.

**Валидация `extraction_schema` в `_build_extraction_model()`** — если Go пришлёт `[{}]` или `["string"]`, Python словит `KeyError`, который не входит в `_NO_RETRY` и уйдёт в 3 ретрая вместо быстрого провала. Добавлена явная валидация с `ValueError` (входит в `_NO_RETRY`): проверка что entry — dict, что `key_name` — непустая строка. Это системная граница (данные от другого сервиса).

**`make ci`** — добавлена цель `format → check → test`. `format` идёт первым, потому что ruff format может исправить то, что `check --fix` не трогает (пробелы, переносы). `check` после этого проверяет без записи. Использовать перед `git push`.

**`Field(..., min_length=1)` vs проверка в `get_client()`** — старый подход (`gemini_api_key: str = ""` + `if not settings.gemini_api_key: raise GeminiAPIError(...)` в `get_client()`) давал ошибку только при первом вызове воркера. Новый подход ломает старт приложения при деплое без переменной — это fail-fast на правильном уровне. Запущенный воркер без ключа хуже, чем не запущенный вовсе.

**`GeminiClient.close()` + `try/finally`** — `httpx.Client` держит пул TCP-соединений и должен быть закрыт явно. В Celery-воркере нет `async with` и lifecycle-хуков, поэтому единственный надёжный способ — `try/finally` на уровне задачи. Синглтон неуместен (Celery fork-безопасность), поэтому `get_client()` создаёт новый клиент per-invocation, и он же должен его закрыть.

## Следующий шаг

Реализовать `workers/parse_invoice._handle` — парсинг XLSX/PDF счетов в структурированные позиции (сейчас `NotImplementedError`).
